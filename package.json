{
  "name": "aihaoban-cn",
  "version": "1.0.0",
  "type": "module",
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "export": "next build && next export", // 关键：静态导出命令
  "deploy": "npm run export && gh-pages -d out"
}
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "antd": "^5.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.3",
    "vite": "^4.4.5"
  }
}


